{% extends 'base.html.twig' %}

{% block title %}Ajouter un utilisateur
{% endblock %}

{% block body %}
	<div class="container mt-5">
		<h2 class="text-primary mb-4">Ajouter un utilisateur</h2>
		<a href="{{ path('app_admin_user_index') }}" class="btn btn-outline-secondary mb-3">← Retour à la liste des utilisateurs</a>

		{{ form_start(form) }}
		{{ form_widget(form) }}
		<button class="btn btn-success mt-3">Créer</button>
		{{ form_end(form) }}
	</div>
{% endblock %}

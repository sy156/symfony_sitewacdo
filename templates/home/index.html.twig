{% extends 'base.html.twig' %}

{% block title %}Accueil
{% endblock %}

{% block body %}
	<div class="container mt-5">
		<div class="row justify-content-center">
			<div class="col-md-8">
				<div class="card shadow-lg border-0 rounded-4">
					<div class="card-body text-center p-5">
						<h1 class="display-5 fw-bold text-primary mb-4">{{ title }}</h1>
						<p class="lead mb-4">
							Bienvenue sur le site back-end Wacdo
						</p>
						<a href="{{ path('app_login') }}" class="btn btn-lg btn-primary">
							Se connecter
						</a>
						<a href="{{ path('app_products') }}" class="btn btn-lg btn-outline-primary">
							Voir les produits
						</a>


					</div>
				</div>
			</div>
		</div>
	</div>
{% endblock %}
